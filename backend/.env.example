# Server
PORT=3001

# PostgreSQL connection
# DB_NAME must be the database that contains the reto_c schema
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=yourdatabase
